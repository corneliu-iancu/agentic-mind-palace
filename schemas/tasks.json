{
  "entity": "tasks",
  "description": "A PARA-style Tasks database with status tracking, energy levels, and scheduling properties",
  "required_properties": {
    "Status": { "type": "status" },
    "Due": { "type": "date" }
  },
  "optional_properties": {
    "Energy": { "type": "select", "options": ["Low", "Normal", "High"] },
    "My Day": { "type": "checkbox" },
    "Location": { "type": "select", "options": ["Computer", "Office", "Home", "Errands"] },
    "Importance": { "type": "select", "options": ["I", "II", "III"] },
    "P-I": { "type": "select", "options": ["Immersive", "Process"] },
    "Tags": { "type": "relation" }
  },
  "match_confidence": {
    "high": "Has Status (type: status) AND Due (type: date) AND at least 2 optional properties",
    "medium": "Has Status (type: status) AND Due (type: date)",
    "low": "Has Status (type: status) OR Due (type: date) with task-like name"
  }
}
